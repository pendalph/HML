import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingTop: 200
  },
  errorText: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#F0928E',
    paddingBottom: 15
  },
  errorDescription: {
    fontSize: 14,
    color: '#F0928E',
    paddingBottom: 30
  }
})